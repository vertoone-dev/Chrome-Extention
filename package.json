{
  "name": "guidesnap-extension",
  "version": "1.0.0",
  "description": "Chrome Extension for recording step-by-step guides",
  "private": true,
  "scripts": {
    "lint": "eslint chrome-extension/",
    "format": "prettier --write chrome-extension/"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.2.0"
  }
}
